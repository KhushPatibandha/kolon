fun: main() {
    var a: int = 0;
    var b: int = 1;
    println(a);
    println(b);

    var n: int = 5;

    for: (var i: int = 2; i < n; i++): {
        var next: int = a + b;
        println(next);
        a = b;
        b = next;
    }
}

