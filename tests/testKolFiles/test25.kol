fun: main() {
    var a: int = 10;
    var b: float = 10.1;
    var c: string = "someString";
    var d: char = 'a';
    var e: bool = true;
    var f: int[] = [1, 2, 3, 4];
    var g: string[int] = {"Khush": 1};
    println(typeOf(a));
    println(typeOf(b));
    println(typeOf(c));
    println(typeOf(d));
    println(typeOf(e));
    println(typeOf(f));
    println(typeOf(g));
}
