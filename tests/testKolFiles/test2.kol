fun: main() {
    var a: int;
    var b: bool;
    var c: int, a, var d: int, b = res();
    if: (c == 1 && a == 2 && d == 3 && b == true): {
        println("true");
        return;
    }
    println("false");
}

fun: res(): (int, int, int, bool) {
    return: (1, 2, 3, true);
}
