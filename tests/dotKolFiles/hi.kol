fun main() {
    var i: int = 10
    var fl: float = 1.1
    var au: auto = "String" // (String)
    var au2: auto = 10 // (int)
    var bo: bool = t
    var bo1: bool = f
}

fun Hehe(n: int): (bool) {
    if: (n == -100) {
        return f
    } else {
        printHello(n)
    }
    return t
}

fun printHello(n: int, s: string) {
    if: (n < 0) {
        io.print("no hehe")
    } elif: (n == 1) {
        io.print("hehe")
    } else {
        loop: (int i = 0, i < n, i++) {
            io.print("hehe")
        }
    }
}

